{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "data.js",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && serve public -l 5500",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.28.2",
    "serve": "^14.2.6",
    "supabase": "^2.114.0"
  }
}
